#ifndef RETURN_H_
#define RETURN_H_

#include "api-parser.h"
#include "config.h"
#include <stdbool.h>

void waybar(struct data *meteo_data, struct config *config_data, bool icon);

#endif // !RETURN_H_
